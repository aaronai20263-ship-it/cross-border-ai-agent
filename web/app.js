const input=document.getElementById('input');const chat=document.getElementById('chat');const imageInput=document.getElementById('imageInput');const leadForm=document.getElementById('leadForm');const success=document.getElementById('success');
function addMessage(text,who='user'){const row=document.createElement('div');row.className=`message ${who}`;row.style.marginTop='18px';if(who==='assistant'){row.innerHTML=`<div class="avatar">玉</div><div class="bubble">${text}</div>`}else{row.innerHTML=`<div class="bubble" style="margin-left:auto;background:#171714;color:#fff;border-radius:18px 4px 18px 18px">${text}</div>`}chat.appendChild(row);chat.scrollTop=chat.scrollHeight}
function reply(text){const safe=text.replace(/</g,'&lt;').replace(/>/g,'&gt;');setTimeout(()=>addMessage(`收到。基于目前信息，我会先从<strong>需求、可见特征和购买风险</strong>三个层面判断。<br/><br/>你可以继续告诉我：预算、用途、想买的产品类型，以及是否已经有具体图片。<br/><br/><strong>重要提示：</strong>仅凭图片无法确认真伪或替代专业鉴定。`, 'assistant'),350)}
document.getElementById('sendBtn').onclick=()=>{const v=input.value.trim();if(!v)return;addMessage(v);input.value='';reply(v)};input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();document.getElementById('sendBtn').click()}});document.querySelectorAll('.quick button').forEach(b=>b.onclick=()=>{input.value=b.dataset.q;document.getElementById('sendBtn').click()});document.getElementById('startBtn').onclick=()=>document.getElementById('consultant').scrollIntoView({behavior:'smooth'});document.getElementById('humanBtn').onclick=()=>document.getElementById('lead').scrollIntoView({behavior:'smooth'});imageInput.onchange=()=>{if(imageInput.files[0]){addMessage(`已选择图片：${imageInput.files[0].name}`);reply('image')}};leadForm.onsubmit=e=>{e.preventDefault();success.classList.add('show');leadForm.reset()};
