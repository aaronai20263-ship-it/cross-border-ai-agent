// No secret/API key belongs in this file.
// Public runtime configuration may be supplied by the hosting layer.
window.JADE_AI_CONFIG = Object.freeze({
  apiBaseUrl: window.JADE_AI_API_BASE_URL || ''
});
